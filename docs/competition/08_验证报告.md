# 复赛作品 · 全量验证报告

- 生成时间：2026-09-21 14:54:48（耗时约 5.1 分钟）
- 运行方式：`python backend/scripts/verify_all.py`（本报告由脚本自动生成，结论未人工修饰）
- 总体结论：**全部通过**（17/17）

## 运行环境（敏感值已脱敏）

| 配置项 | 取值 |
|---|---|
| `LLM_API_KEY` | （已配置） |
| `LLM_BASE_URL` | https://api.deepseek.com |
| `LLM_MODEL` | deepseek-v4-flash |
| `LLM_JSON_MODE` | true |
| `LLM_DISABLE_THINKING` | false |
| `VISION_PROVIDER` | minimax |
| `VISION_BASE_URL` | https://api.minimaxi.com/v1 |
| `VISION_API_KEY` | （已配置） |
| `VISION_MODEL` | MiniMax-M3 |
| `VISION_FALLBACK_PROVIDER` |  |
| `VISION_FALLBACK_BASE_URL` | https://qianfan.baidubce.com/v2 |
| `VISION_FALLBACK_API_KEY` | （未配置） |
| `VISION_FALLBACK_MODEL` |  |
| `LLM_REVIEW_ENABLED` | false |
| `LLM_REVIEW_MODEL` | Qwen2.5-72B-Instruct |
| `LLM_REVIEW_BASE_URL` | http://192.168.1.231:30001/v1 |
| `LLM_REVIEW_API_KEY` | （未配置） |
| `RBAC_ENABLED` | false |
| `DECISION_PROVIDER` | none |
| `DECISION_BASE_URL` | https://api.typesafe.ai |
| `DECISION_API_KEY` | （未配置） |
| `DECISION_MODEL` | jev-latest |
| `DECISION_ENABLED` | false |
| `DECISION_HIGH_CONF` | 0.9 |
| `DECISION_LOW_CONF` | 0.5 |

## 验证明细

### ✅ 服务级 · 五支柱（0s）

```text
[PASS] 命中历史承办集合 ≥ 90%（17/18 = 94.4%）
[PASS] 依据链含政策依据与属地判定
[PASS] 属地主办路径正确（镜湖区政府） — 镜湖区政府
[PASS] 专业直派路径正确（公交） — 专业直派/市公安局
[PASS] 「小区燃气管道破裂有燃气味…」→ 特急 — 实际=特急｜建议 30 分钟内电话通知承办单位并派件，24
[PASS] 「整栋楼停水两天了，居民无…」→ 紧急 — 实际=紧急｜建议当日派件，24 小时内反馈进展（依据：规范紧
[PASS] 「我想咨询一下营业执照怎么…」→ 一般 — 实际=一般｜规定时限内办结；复杂事项可申请延期或「承诺办理」
[PASS] 「井盖丢失，晚上有人差点掉…」→ 特急 — 实际=特急｜建议 30 分钟内电话通知承办单位并派件，24
[PASS] 理解节点标记紧急 → 至少紧急级 — 紧急
[PASS] 分级含政策依据
[PASS] 识别重复诉求标记
[PASS] 识别退回风险并给建议
[PASS] 清晰权责不误报退回风险
三支柱验证：全部通过
```

### ✅ 服务级 · 答复合规审查（2s）

```text
[PASS] 政策引用不可溯          → 风险 高（政策引用不可溯）
[PASS] 合规#1  → 风险 无（—）
[PASS] 合规#2  → 风险 无（—）
[PASS] 合规#3  → 风险 无（—）
[PASS] 合规#4  → 风险 无（—）
[PASS] 合规#5  → 风险 无（—）
[PASS] 合规#6  → 风险 无（—）
[PASS] 合规#7  → 风险 无（—）
[PASS] 合规#8  → 风险 无（—）
[PASS] 合规#9  → 风险 无（—）
[PASS] 合规#10 → 风险 无（—）
违规召回率：10/10 = 100%（阈值 ≥90%）
合规误报率：0/10 = 0%（阈值 ≤10%）
答复合规审查验证：PASS
```

### ✅ 服务级 · 办理时限（0s）

```text
[PASS] 特急超 25h → 超期 — 超期
[PASS] 超期附督办建议 — 已超期：建议按规范启动督办（群众多次反映、承办单
[PASS] 特急剩 4h → 临期 — 临期 剩4.0h
[PASS] 紧急剩余 14h → 正常 — 正常
[PASS] 一般件 5 个工作日（周五+5 → 下周五） — 2026-09-25T09:00
[PASS] 一般件模式为工作日 — 工作日
[PASS] 一般件时限标注为建议值 — 未配置法定节假日数据，仅按周末计算工作日（节假日顺延未计入）；一般件时限为建议值（待与芜湖市办理细则核对）
[PASS] 未配置法定节假日时显式提示 — 未配置法定节假日数据，仅按周末计算工作日（节假日顺延未计入）；一般件时限为建议值（待与芜湖市办理细则核对）
[PASS] 一般件附承诺办理说明 — 复杂/历史遗留事项可申请「承诺办理」，最长不超过
[PASS] 一般件 8 个自然日后 → 超期 — 超期
[PASS] 已办结案件不再计超期 — 已办结
[PASS] 节假日顺延（跳过 9/21 假日 → 9/28） — 2026-09-28T09:00:00
[PASS] 调休工作日计入（9/19 周六计为工作日 → 9/24） — 2026-09-24T09:00:00
时限倒计时验证：12/12 通过
```

### ✅ 服务级 · 第二模型交叉复核（2s）

```text
[PASS] 未启用时优雅返回不可用 — 未启用第二模型复核（LLM_REVIEW_ENABLED/LLM_REVIEW_MODEL）
（如需完整验证：.env 设 LLM_REVIEW_ENABLED=true 后重启）
[PASS] 复核统计接口可用 — 200
已复核案件 2｜一致 2｜分歧 0｜一致率 1.0｜人工介入率 0.0
[PASS] 统计含配置与样本
[PASS] 统计口径自洽（一致+分歧=已复核）
[PASS] 时限督办统计可用
[PASS] 时限分布含四类状态
交叉复核验证：PASS
```

### ✅ 服务级 · 图片证据受理机制（36s）

```text
[PASS] 升级标注图片证据来源 — 图片证据：井盖缺失（按险种定级）
[PASS] 视觉未判隐患 → 不升级 — 一般
[PASS] 视觉服务可用
[PASS] 上传接口返回 200 — 200
[PASS] 视觉返回隐患类型
[PASS] 视觉返回严重程度 — 一般
[PASS] 建单返回 200 — 200
[PASS] 案件带回附件与视觉结论
[PASS] 未判隐患时视觉未触发升级（文本规则另计） — 急件=紧急（可能由文本规则判定）
[PASS] 时限条款与等级匹配 — 对大面积停水停电、环境污染等紧急事项，承办单位需第一时间
[PASS] 隐患类答复含安全提示
[PASS] 原图可回显 — 2676 bytes
[PASS] 案件附件列表可查 — 1 条
图片受理端到端：PASS
```

### ✅ 接口级 · 流转状态机（0s）

```text
[PASS] 接口返回状态与迁移 — 10 状态 / 15 迁移
[PASS] 看板返回全部分组 — 10 组
[PASS] 看板案件含时限状态
[PASS] 历史案件默认已受理 — 70
[PASS] 案件流转查询可用
[PASS] 流转历史为列表 — 0 条
[PASS] 非法迁移返回 409 — 不允许从「已受理」迁移到「已归档」；合法目标：['已分类（待审核派单）']
[PASS] 越权派单返回 409 — 角色 agent 无权执行「确认派单」（需要：dispatcher/admin）
[PASS] 派单员派单成功 — 已派单
[PASS] 流转历史记录操作者与角色 — 2 条记录
[PASS] 主线全链路迁移（签收→办理→回执→审核→归档）
[PASS] 最终状态为已归档 — 已归档
[PASS] 流转记录完整（≥7 条） — 7 条
流转状态机验证：PASS
```

### ✅ 接口级 · wiki 词条（0s）

```text
[PASS] 返回修订历史（2 版） — 2 版
[PASS] 默认状态为草稿 — draft
[PASS] 可读取历史版本正文 — 首次建档
[PASS] 发布成功 — 200
[PASS] 发布后状态生效 — published
[PASS] 检索到已发布词条 — 1 条
[PASS] 列表可用且含分类 — 政策解读、部门职责、办事指南、案例经验、口径话术
[PASS] 已发布计数 ≥1
[PASS] 案件沉淀为词条草稿 — 200 case-c6015efe-public_safety
[PASS] 草稿含承办单位与答复口径 — ## 事项类别
[PASS] 非法 slug 被拒（400） — 400
[PASS] 非法分类被拒（400） — 400
[PASS] 词条操作写入审计 — 保存词条、删除词条、发布词条、沉淀词条
wiki 验证：PASS
```

### ✅ 接口级 · RBAC 与审计（0s）

```text
[PASS] 已初始化账号 — 2 个
[PASS] 角色矩阵有 6 个角色 — 坐席、派单员、审核员、部门用户、管理员、监督员
[PASS] 关闭态：无需鉴权即可查询账号 — 200
[PASS] 审计日志可查询 — 115 条
RBAC 验证（关闭态）：PASS
```

### ✅ 接口级 · 阶段A端到端（0s）

```text
[PASS] 案件返回 reply_audit
[PASS] 含风险等级 — 无
[PASS] 含政策依据条款
[PASS] 案件返回 deadline
[PASS] 含到期时刻与状态
[PASS] 含依据条款
[PASS] 清单接口可用
[PASS] 能识别超期案件（历史案件） — 50 条
[PASS] 清单含督办建议
阶段A端到端：PASS
```

### ✅ 单元测试（15s）

```text
3 passed, 5 warnings in 12.05s
```

### ✅ 评测 · 事项分类留一验证（82s）

```text
===== LOO 分类准确率：18/18 = 100.0%（80s）=====
```

### ✅ 评测 · 派单策略对照（0s）

```text
类别         实际承办（历史）               通用做法预测               三层决策预测             路径 命中
通用做法（按类别推市直部门）主办命中：1/18 = 5.6%
三层决策（专业直派/属地主办/类别兜底）主办命中：17/18 = 94.4%
```

### ✅ 评测 · 图片隐患识别（77s）

```text
隐患类型准确率：15/16 = 93.8%
严重程度准确率：11/16 = 68.8%
隐患有无一致率：16/16 = 100.0%
```

### ✅ 界面 · 工作台交互（20 项）（56s）

```text
[PASS] 链路完成→待审核
[PASS] 审核卡×4（确认按钮数） — 4 个
[PASS] QC 面板
[PASS] 效率条
[PASS] 最终放行按钮
[PASS] 轨迹抽屉打开
[PASS] 存在确认按钮 — 4 个
[PASS] 确认后状态变化
[PASS] 知识库页打开
[PASS] 减负账本
[PASS] 政策库面板
[PASS] 法规全文库 ≥3 部可见 — 4/4 部
[PASS] 无控制台严重错误
点检结果：20 通过 / 0 失败
```

### ✅ 界面 · 流转看板（13s）

```text
[PASS] 包含分组「办理中」
[PASS] 包含分组「已回执」
[PASS] 包含分组「审核通过」
[PASS] 包含分组「已归档」
[PASS] 显示超期督办提示
[PASS] 显示操作身份选择器
[PASS] 卡片带可执行迁移按钮 — 52 个
[PASS] 点击「确认派单」后看板刷新（无报错）
[PASS] 刷新后看板仍正常
[PASS] 看板状态与点击前不同（发生迁移）
[PASS] 详情页含工单流转条
[PASS] 流转条含主线进度
[PASS] 控制台零严重错误
看板界面验证：19 通过 / 0 失败
```

### ✅ 界面 · 词条与督办（14s）

```text
[PASS] 看板页含督办面板
[PASS] 显示四类时限分布
[PASS] 显示最紧迫案件
[PASS] 知识库页含「知识词条」面板
[PASS] 显示词条统计
[PASS] 含口径层说明
[PASS] 出现新建表单
[PASS] 保存成功并显示版本
[PASS] 列表出现该词条
[PASS] 发布成功（状态生效）
[PASS] 显示修订历史
[PASS] 控制台零严重错误
词条/督办界面验证：12 通过 / 0 失败
```

### ✅ 界面 · 账号与审计（6s）

```text
[PASS] 导航含「账号与审计」
[PASS] 显示当前身份卡
[PASS] 显示角色权限表
[PASS] 演示态说明可见
[PASS] 无需登录即可查看审计
[PASS] 审计记录非空 — 80 条
[PASS] 审计含操作者信息
[PASS] 控制台零严重错误（忽略预期的鉴权拒绝）
账号与审计界面验证：8 通过 / 0 失败
```

## 指标口径说明（备评委追问）

| 指标 | 口径 |
|---|---|
| 事项分类 100% | 官方 18 条样例**留一交叉验证**：预测某条时，few-shot 池排除该条自身，防答案泄漏 |
| 派单 94.4% | 同一批样例，对照其**历史实际承办单位**；通用做法（按类别推市直部门）同集为 5.6% |
| 图片隐患 93.8% / 有无 100% | 16 张 Wikimedia 公开许可照片，评测时**不向模型透露期望答案**；严重程度因跨次不稳定，改为按险种定级 |
| 答复合规 召回 100% / 误报 0% | 10 条构造违规 + 10 条构造合规答复；规则为确定性主判 |
| 时限 12/12 | 含工作日推进、周末与节假日顺延、调休、临期/超期/办结 |

> 说明：涉及生成模型的服务级评测使用文本构造样本与既有案件，不依赖外部服务可用性；
> 图片评测依赖视觉模型配额，若配额不足该项可能失败，属环境问题而非逻辑缺陷。
